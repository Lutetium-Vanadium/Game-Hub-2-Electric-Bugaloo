import keys from "./keys";
import gameConstants from "./gameConstants";

const {
    WIDTH,
    HEIGHT,
    FPS,
    BLOCK_SIZE,
    INITIAL_SNAKE,
    PADDING,
    GRID_BLOCK_SIZE
} = gameConstants;

export {
    keys,
    WIDTH,
    HEIGHT,
    FPS,
    BLOCK_SIZE,
    INITIAL_SNAKE,
    PADDING,
    GRID_BLOCK_SIZE
};
